version: "3.9"

services:
  backend:
    container_name: "test-api-oprec"
    image: "oprec-api-test"
    volumes: ["./:/home/bun/app"]
    restart: unless-stopped
    networks:
      - cf-tunnel

networks:
  cf-tunnel:
    external: true
